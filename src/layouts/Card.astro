---
title: "Card";
description: "A Card component for displaying content in a card-like format.";

const {
  className,
}: {
  className?: string;
} = Astro.props;
---

<div
  class={`flex mb-10 backdrop-blur-lg bg-slate-800 bg-opacity-60 rounded-3xl p-10 shadow-xl max-w-lg ${className}`}
>
  <slot />
</div>
